<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
			<div class="field">
				<%=  f.label :reason, 'Reason for this Report' %><br />
				<%=  text_area( :reportblockcontent, :reason)%>
			</div> 
			<div class="field">
				<%=  f.label :email, 'Email Address (optional)' %><br />
				<%=  text_field( :reportblockcontent, :email)%>
			</div>  			
			<%= text_field( :reportblockcontent, :status, :value => 0, :style => "display:none")  %>	
			<%= f.submit 'Report Company' %> </br></br>			

<% end %>

