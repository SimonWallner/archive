When /^I fill in the (.*) video field with a youtube embed code$/ do |count|
    if count == "first"
        puts "first"
    end
end

Then /^I should see the embedded video on the details page of the game$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I fill in the first field with a youtube embed code$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^I should not see a second field for the embed code$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I click the add video button$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^I should see a second field for the embed code$/ do
  pending # express the regexp above with the code you wish you had
end



Then /^I should see the embedded videos on the details page of the game$/ do
  pending # express the regexp above with the code you wish you had
end
